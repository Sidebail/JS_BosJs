extends layout

block content
  h1= title
  p Welcome to #{title}
  p Input yout nickname:
  // turn into form with post method
  form(method="post")
    input(type="text" name="nickname")
    button(class="btn btn-primary") Play Game